name: hand_cricket
description: "Flutter Hand Cricket application"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter
  flutter_svg: ^2.0.17
  flutter_bloc: ^9.1.0
  cupertino_icons: ^1.0.8
  rive: ^0.13.20
  shimmer_border_container: ^1.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
flutter:
  uses-material-design: true
  assets:
    - assets/

